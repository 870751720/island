# 道具图标体系

## 背景

道具图标原先全部使用 emoji(定义在 `ITEMS` 的 `icon` 字段),存在两类问题:

1. **图标与道具不搭配**:巨鲶用河豚 🐡、剑鱼用匕首 🗡️、橡果用花生 🥜、松果用整棵树 🌲、烤大鱼用团子 🍡 等。
2. **图标重复**:树枝/植物纤维/灌木丛同为 🌿,弓与箭同为 🏹,床和工作台多级共用同一图标且无法区分级别。

## 需求描述

- 每个道具的图标要贴切表达道具本身,且**互不相同**。
- 多级别道具(一~四级工作台、床、精致工具)需在图标上**标出级别**。
- emoji 找不到贴切表达的道具,用自绘 SVG 图标补位。

## 设计方案

- **统一渲染入口** `src/ui/ItemIcon.tsx`:所有道具图标的 React 渲染都走 `<ItemIcon kind size level? />`。
  - 优先查自绘图标表 `src/ui/icons/CustomIcons.tsx`,没有则回退渲染 `ITEMS[kind].icon` 的 emoji;
  - 级别角标:按 `workbenchItemLevel`/`bedItemLevel` 从 kind 推导,`level` 参数可覆盖(如背包工具页的精致工具角标 2),右上角绿色圆形数字。
- **自绘图标**(`CustomIcons.tsx`,64×64 视口纯色简笔,呼应黏土质感):橡树种子(带嫩芽,与橡果 🌰 区分)、松果、无糖可乐(黑色易拉罐,与可乐 🥤 区分)、泥鳅、石斑鱼、巨鲶(长须)、剑鱼(长吻)、魔鬼鱼、黄金鱼、烤浆果、烤大鱼(烤痕)、箭(与弓 🏹 区分);鱼形共用一个 `FishShape` 基础组件按参数拼装。
- **emoji 调整**(`ITEMS`):树枝 🌿→🪾、植物纤维 🌿→🌾、灌木丛 🌿→🌳、围栏门 🪵→🚪、橡果 🥜→🌰;`FOODS` 的橡果同步。
- **配方图标去冗余**:`Crafting.ts` 删除 `Recipe.icon` 字段,新增 `recipeIconKind`/`recipeIconLevel` 按产物推导,精致工具配方自动带 2 级角标。
- **拾取提示**:头飘 toast 的数据从 `{icon,count}` 改为 `{kind,count}`(按道具合并),由 GameplayUI 用 `ItemIcon` 渲染。
- 纯文本场合(3D 头顶进度提示)保留 emoji 回退,统一取自 `ITEMS[kind].icon`(Food/Wine 不再有独立 icon 字段);小地图 canvas 标记(工作台/火堆/床)为场景概念,维持独立 emoji。

### 作物种子自绘图标(2026-09-10)

- 12 种作物种子全部改为自绘 `CropSeedIcon`(土色种粒 + 嫩芽 + 作物色果标记,按 round/taper/cob/bean/berry/head/big/potato/egg/grain 十种轮廓参数化区分),解决多个种子共用 🌱、麦种与植物纤维共用 🌾 的重复;emoji 仅作纯文本回退。

## 迭代记录

### 2026-09-12 皮铁装与部分鱼类自绘(选型定稿)

- 经 HTML 选型页由玩家挑选定稿:
  - 皮衣:长袖皮衣(奶油袖口铜扣);皮裤:深棕短裤奶油卷边;皮帽:报童帽短鸭舌;皮包:棕皮手提包。
  - 铁甲:长袖海沫水手衫;铁裤:海沫短裤奶油卷边;铁帽:奶油水手帽海沫帽带;铁包:帆布手提包海沫顶盖。
  - 沙丁鱼:细长银身浅腹;鲈鱼:草绿棘背金腹。
- 浆果、可乐、无糖可乐、虾仍用 emoji,待二次选型后再改自绘。emoji 仍作纯文本回退。

### 2026-09-12 基础材料自绘

- 树枝、燧石、铁矿石、铁锭、植物纤维、皮毛改为自绘 SVG,替换不贴切的 emoji(树枝 🪾、燧石 ⚡、铁矿石 💎、铁锭 ⚙️、纤维 🌾、皮毛 🐾)。
- 定稿造型:树枝弯枝分杈、燧石剥片顶焰、铁矿石嵌金属角砾、铁锭两块叠锭、植物纤维宽叶三片、皮毛毛面衬里。emoji 仍作纯文本回退。

### 2026-09-12 手持工具图标与背包图标统一

- 新增 `ToolIcon`(`ItemIcon.tsx`):手持工具图标统一入口,有对应道具的工具一律走 `ItemIcon`(自绘/emoji 与背包一致),空手/围栏等非道具型手持项用回退 emoji。
- `ToolButton` 删除 `TOOL_ICONS` emoji 表和锄头单独特判;手持项选择面板(`PlacePicker`/`placePickerItems`)的 `icon` 改为 ReactNode 并统一走 `ItemIcon`/`ToolIcon`;可放置道具在工具按钮上的图标也走 `ItemIcon`。

### 2026-09-12 蚯蚓、经验书、工具与草装自绘(选型定稿)

- 覆盖 14 个道具:蚯蚓、冒险家的经验书、木斧、木镐、木铲、木锄、树枝鱼竿、树枝弓、木剑、草衣、草裤、草帽、草包,以及铁锭换成新造型。
- 经 HTML 选型页(`docs/icon-picker.html`,每项 18 种方案)由玩家挑选定稿:
  - 铁锭:经典梯形锭(顶面亮、侧面暗);蚯蚓:叶片旁 C 形粉蚯蚓;经验书:蓝皮书 + 斜插羽毛笔。
  - 木斧:竖柄石斧月牙刃绑绳;木镐:月牙双尖镐头垂直穿柄;木铲:木柄 + 套管 + 梯形圆弧铲刃;木锄:长柄底部垂直伸出的锄板(刃在板外缘)。工具形按真实结构绘制(斧眼/镐眼/L 形锄板)。
  - 鱼竿:弯竿钓起小鱼 + 水波;弓:弯弓搭箭带羽尾;剑:十字护手金柄剑。
  - 草装为绿色系配色:草衣圆领上衣(苔绿)、草裤缀叶(苔绿)、草帽宽檐(嫩绿)、草包网兜提包(嫩绿)。
- 同步更新了已有自绘的锄头、草衣、草裤、铁锭组件造型。emoji 仍作纯文本回退。

### 2026-09-12 图标来源全量统一到道具配置

- 删除 `Food.ts`(FOODS)与 `Wine.ts`(WINES)的平行 `icon` 字段,进食 3D 头顶提示改取 `ITEMS[kind].icon`,消除「UI 自绘 / 提示 emoji」两套表现。
- `ItemIcon` 的 `level` 参数支持传 `null` 显式关闭级别角标(设施类按钮/标题用)。
- 设施入口图标全部走 `ItemIcon`:`ToolButton` 靠近设施时的按钮图标(工作台/火堆/木箱/饵料桶/酿酒桶/冶炼炉/烹饪台/纺织机/床;打桩/解绳无独立道具,统一用套索图标),以及各设施面板标题(含 CratePanel 铁箱/木箱区分)。
- `BaitBarrelPanel` 文案中的 🪱/🌱 硬编码改为 `ItemIcon`(鱼饵 `bait` / 南瓜种子 `pumpkinSeed`,与桶的实际产出一致);`ConvertRow` 的 `hint` 改为 ReactNode 以支持内嵌图标。
- `BottleMessage` 的 `icon`(emoji 字符串)改为 `kind`(ResourceKind),内部走 `ItemIcon`,漂流瓶/海神的信与道具配置一致。

### 2026-09-12 黏土风第二批选型定稿(基础材料/食物/饮品)

- 经四轮 HTML 选型页(`docs/icon-preview.html`,17 个物品 10~35 种方案)由玩家挑选,14 个道具定为黏土质感自绘:木头、绳线、布料、浆果、可乐、无糖可乐、河豚、蟹肉、鸟肉、烤大鱼、烤黄金鱼、烤蟹肉、烤鸟肉、兽肉;虾、墨鱼、烤兽肉保留 emoji 不改。
- 新增 `src/ui/icons/ClayIcons.tsx`:黏土风图标独立模块(64×64 视口,软色块 + 暗部弧 + 高光 + 落影,原始 SVG 静态字符串经 `dangerouslySetInnerHTML` 注入按 viewBox 缩放),导出 `CLAY_ICONS` 由 `CustomIcons` 合并进 `CUSTOM_ICONS`。
- 造型要点:兽肉/鸟肉同为生肉红且鸟肉个头更小;烤制品带烤签;无糖可乐与可乐靠罐身配色区分。旧的简笔版无糖可乐、烤大鱼组件随之删除。
- 联机无影响:图标为纯客户端表现,快照/动作只传 kind。
