# 多人联机(WebRTC 房主权威)

## 背景

游戏目前是纯单机:一个 `Game` 实例绑定唯一 `Player`,玩法判定、存档、HUD 全部围绕单玩家。目标是支持多人联机共玩同一座岛(人数不设上限),且不引入任何游戏服务器、保持 GitHub Pages 纯静态部署。

## 需求描述

- 多人(2 人即可开玩,人数不设上限)在同一个房主权威世界中共同生存；岛形由地形种子生成，自然资源由房主持有并同步完整状态。
- 传输:WebRTC DataChannel 玩家直连(房主星形拓扑),第三方服务不承载游戏数据。
- 信令:五位数字房间码或二维码,通过国内公共 MQTT 自动交换 WebRTC 握手信息。
- 权威模型:房主客户端跑完整玩法模拟;客人只跑表现层 + 自身移动预测。
- 存档:联机时房主持档;单机模式保持现状;存档结构变化时 SAVE_VERSION +1。
- 断线:房主退出即全房解散;客人断线由房主按超时移除该会话。

## 设计方案

### 模块划分

- `src/game/net/`:消息协议(`Protocol`)、WebRTC 连接(`PeerNet`)、在线房间信令(`Signaling`)、房主与客人会话总管。
- `src/game/mp/`:每玩家会话(`PlayerSession`:player + survival + inventory + equipment + tools)、远程玩家驱动(`RemoteDriver`)、世界状态同步(`WorldSync`)。

### 权威与同步

- 房主侧为每名客人建一个 `PlayerSession`(复用本地 Player 物理与动画,输入由网络摇杆写入 `player.input.setJoystick`,保证与本地移动完全一致的判定)。
- 客人侧:收到地形种子后重建相同岛形，再按房主完整资源列表建立资源布局；远程玩家用 Player 的「遥控模式」(`setNetPose` 插值)表现；权威状态(资源点/掉落物/放置物/动物/昼夜/天气)由 `WorldSync` 增量应用。
- 消息:客人→房主 `input`(摇杆)与 `action`(按钮语义,即 Game 公共方法参数化);房主→客人 `welcome`(种子+初始状态)、`snapshot`(10–15Hz)、`event`(音效/特效/拾取/死亡)。

### 里程碑

- M1 多玩家抽象:Player 遥控模式、PlayerSession、Game 会话化、动物按最近玩家结算(单机行为不变)。
- M2 状态收拢与实体 id:按会话结算的交互系统(采集/制作/火堆/木箱/钓鱼等)、掉落物与动物 id、SAVE_VERSION=26、权威随机收敛。
- M3 网络层:PeerNet/RoomCode/NetSession、房主权威模拟、客人 guest 模式。
- M4 联机 UI:房间创建/加入、lobby、断线处理。
- M5 打磨:名字标签、事件同步、死亡规则联机化。

## 迭代记录

### M22：实时姿态与 HUD 字段增量

- 玩家、动物、螃蟹、鸟、蝴蝶和宠物狗不再按 100ms 整表广播；以稳定 ID 维护接收端镜像，只发送发生变化的字段，静止实体为零流量。
- 坐标、朝向和连续生存数值按画面可感知精度量化，过滤浮点微抖；实时通道每 5 秒发送一次恢复关键帧，使偶发丢包能够自动收敛。
- HUD 是客人专属的生存、背包、装备、制作及附近交互状态。现在按顶层字段可靠增量发送，背包等大字段只有内容变化时才传输。
- 客人摇杆输入只在方向变化时发送，静止不再每秒发送 20 个相同输入包；可靠心跳仍每秒维持连接。
- 协议版本升级为 10，不同版本客户端会明确提示刷新，避免增量协议被旧客户端误读。

### M23：玩家与战斗动物 25Hz

- 玩家姿态与具有主动攻击能力的动物（狼、熊）由 10Hz 提高到 25Hz，降低移动、追击和攻击表现延迟。
- 兔、羊、鹿及环境生物保持 10Hz，狼与熊等主动攻击动物走高频战斗姿态通道，HUD 保持 5Hz 变化检查；所有通道继续只发送量化后的字段变化，静止对象不因调度提频增加日常流量。
- 5 秒恢复关键帧保持不变，用于修复实时不可靠通道的偶发丢包。

### M24：熊普通攻击动作可靠同步

- 熊开始普通挥击时，房主立即通过可靠控制通道广播一次带稳定动物 ID 的 `wildlifeAttack` 事件；客人端收到后直接触发对应熊的 0.35 秒前顶挥击动画，不等待姿态快照，也不会因短时状态跨过采样窗口而丢失。
- 熊扑击落地时，房主同步广播权威落点，客人端在该坐标补播与房主一致的 10 粒子扬尘特效。
- 攻击动作不加入 25Hz 姿态包，静止或未攻击时不产生额外流量；伤害仍由房主权威结算。联机协议版本升至 11，避免新旧页面混连。

### M20 自然资源改为房主权威完整状态（2026-09-05）

- 移除 `propsSeed`：新世界的自然资源只在房主/单机端首次随机生成，不要求各端通过伪随机序列复算相同布局。
- 每个资源点直接同步种类、坐标、朝向、采集状态和树木生长状态；客人收到欢迎包或 `world.props` 增量时按完整列表重建。
- 被锄头挖走的资源直接从房主世界列表删除，`PropState/PropSave` 不再保存 `dug` 墓碑。房主后续下发的新列表自然完成客人端删除。
- 存档版本升级到 28，联机协议升级到 7。
- 2026-09-04:客人端拾取飘字修复——客人端背包由 HUD 快照 `load` 回填、不触发 `Inventory.onAdd`,现于 `netApplyHud` 中对比快照前后数量差补发拾取提示,且 `flushPickups` 客人端同样执行;拾取提示数据由 `{icon,count}` 改为 `{kind,count}`(两端共用 `ItemIcon` 渲染,含自绘 SVG 与级别角标)。

### M1 多玩家抽象(2026-08-31)

- `MoveInput` 支持 `attach=false`(远程玩家实例不挂键盘监听);`Player` 构造增加 `{ remote }` 选项:遥控模式下不读本地输入,位置/朝向由 `setNetPose` 写入的目标姿态插值,移动感由剩余距离推出以驱动走路动画;水面/游泳/作业动画复用本地逻辑。
- 新增 `src/game/mp/PlayerSession.ts`:每玩家个人状态包(player/survival/inventory/equipment/tools)。
- `Game` 持有 `sessions[]`(下标 0 为 `local`),原 `player/survival/inventory/equipment/tools` 字段改为委托到本地会话的 getter,单机路径全部不变;新增 `addRemoteSession/removeRemoteSession/sessionOf`。
- 帧循环遍历所有会话更新玩家实体;`Wildlife` 改为对最近玩家反应(感知/追击/扑击),熊命中按目标玩家所属会话结算(减伤、掉血、减速),伤害数字仅飘在本地玩家头顶。
- 采集/制作/火堆等交互系统仍绑定本地玩家,按会话结算留待 M2;螃蟹/博美/蝴蝶/鸟仍跟随本地玩家,留待后续里程碑。

### M2 状态收拢与实体 id(2026-08-31)

- 新增 `src/game/mp/Actor.ts`(`Actor` 接口:player/inventory/survival/tools),`PlayerSession` 实现该接口,并挂上每会话独立的玩家侧交互系统实例(采集/制作/进食/钓鱼/弓/喝水,由 Game 的 `attachSessionSystems` 装配);受击音节流、死亡沿等个人计时也移入会话。
- 世界侧系统(掉落物/木箱/床/火堆/工作台/围栏)保持世界单实例,但全部交互方法改为接收发起者 `PlayerSession` 参数,每玩家的搭建/烹饪/挖掘/睡觉/放置进度存系统内 `Map<PlayerSession, State>`;`updateActor` 按会话推进,`update` 只推进世界部分(火堆燃烧、门开合);围栏落点预览改为按会话各持一份;`detach` 在移除会话时清理。
- `Game`:新增统一的 `isSessionBusy(session, exclude)` 占用判定(排除询问方自身);主循环按会话结算生存值/受击/死亡/交互;全部 HUD 按钮方法增加可选 `actor` 参数(默认本地会话),为 M3 房主代客人发起动作铺路;HUD/相机/头顶指示仍绑定本地会话。
- 存档:`SAVE_VERSION` 升至 26(不兼容旧档,遵守无兼容约定);新增 `SessionSave` 结构,`SaveData.others` 保存联机时房主持有的远程玩家会话,恢复时按接入顺序重建远程会话。
- 同步 id:掉落物(`DropSystem` 内 `nextId`)与野生动物(`Wildlife` 内 `nextId`,重生复用同一 id)补短 id;放置物(火堆/工作台/木箱/床/围栏)以落点坐标天然可对应,不加 id。
- `WorldSync` 增量结构与权威随机收敛移至 M3(与 NetSession 消费方一起落地,避免先写无消费者的死代码);螃蟹/博美/蝴蝶/鸟仍跟随本地玩家,留待后续里程碑。

### M3+M4 网络层与房间流程(2026-08-31)

- 新增 `src/game/net/`:`Protocol`(消息判别联合:hello/welcome/start/input/action/players/animals/world/hud)、`RoomCode`(SDP 邀请码/回传码编解码,JSON→base64url)、`PeerNet`(单条 WebRTC DataChannel 直连,Google 公共 STUN,ICE 收集完成或 4s 超时后出码)、`Actions`(客人动作→Game 方法的参数化分发表,以该客人的会话为 actor)、`NetHost`(房主侧:多连接管理、10s 无消息判断线、100ms 一拍广播玩家/动物快照、200ms 一拍按会话下发 HUD、1s 检查世界脏并全量重放)、`NetGuest`(客人侧:20Hz 摇杆上行、动作上行、下行数据交给 Game)。
- `Game` 增加 `GameOptions`(host/guest/seeds/save):房主开新岛(seeds 来自大厅,不读旧档)、客人用欢迎包的种子与全量初始状态重建世界,不读写 localStorage;guest 模式跳过全部权威模拟(生存结算/交互推进/自动换工具/HUD 推送/存档),新增 `netApplyPlayers/netApplyAnimals/netApplyWorld/netApplyHud` 应用下行数据;自己的移动本地预测(完整物理),偏差超 3 格才校正;其他玩家走遥控插值。约 30 个动作方法在 guest 模式下转发给房主(`selectTool` 同步本地预测)。
- `pushHud` 拆出 `snapshotHud(session)`,房主为每个客人各算一份 HUD 下发;世界系统(火堆/工作台/木箱/床/围栏/掉落物)补 `clear()`,客人收到世界快照时清场重放;资源点 `applySave` 原地更新。`Wildlife` 补 `netPoses/netApply`。房主端远程玩家 `keyboard:false`(只吃网络摇杆,屏蔽本机键盘)。
- 新增 `src/ui/RoomLobby.tsx` 大厅:房主逐个朋友「生成邀请码→粘贴回传码」(最多可继续邀请,开始后也可中途加入);客人「粘贴邀请码→回传码发回→等待房主开始」。`StartScreen` 增加「创建房间/加入房间」入口,`GameCanvas` 阶段路由扩展,退出时断开联机会话。
- 修复 M2 引入的两处回归:主循环丢失 `updateAutoEquip` 与 `mumbles.update` 调用。
- 已知留待 M5:客人端无动作音效/特效事件、瓶中信在客人端不可用、围栏门对客人不自动开、死亡规则仍为「本地死亡即清档」(客人不清档)、名字标签未做。

### M4.1 联机稳定性与大厅交互(2026-09-02)

- 修复 DataChannel 打开前 `hello` 被丢弃的问题：网络层缓存待发消息，连接建立后按顺序发送，因此开局前和游戏中途加入都能完成欢迎流程。
- 玩家身份由会随断线变化的数组下标改为稳定 UUID；快照会自动补建新玩家、移除已离开的玩家，前序客人退出不再导致剩余客人身份错位。
- 世界脏快照改为每秒只序列化一次并广播给全部客人，修复三人以上时只有第一名客人收到世界变化的问题，同时减少重复序列化。
- 房间严格限制为房主加三名客人；客人掉线或房主退出后返回开始界面并显示原因。
- 手机大厅改为两步引导，加入系统分享、一键复制、读取剪贴板、等待状态和已连接人数反馈；房主至少连接一名朋友后才能开始。
- 按项目要求，本轮不新增自动化测试；验收仅执行 TypeScript/生产构建。

### M5 联机体验收尾(2026-09-02)

- 玩家快照加入昵称，其他玩家头顶用单个 CanvasTexture 精灵显示名牌；自己的名牌隐藏，避免遮挡作业进度与自言自语，多名玩家的额外 drawcall 保持可控。
- 新增轻量 `event` 消息。房主在权威动作结算时广播音效语义与发生坐标，客人补播程序化音效和小型粒子反馈；高频姿态快照与低频世界存档不再承担瞬时反馈。
- 漂流瓶纳入权威动作：客人点击使用后由房主扣除物品并只向该玩家返回瓶中信内容，背包数量继续由 HUD 快照回流。
- 围栏门的自动感应改为显式使用全部玩家位置；客人端不运行权威交互系统时，门也会根据同步到的玩家位置正常开合。
- 死亡按玩家个人结算：客人死亡只结束自己的游玩，不清房主存档；房主侧只要仍有队友存活就继续自动保存。单机仍保持死亡清档规则，房主离开则因 WebRTC 星形拓扑结束房间。
- 按需求不新增或运行自动化测试；仅执行 TypeScript/生产构建验收。

### M6 稳定性与同步收尾(2026-09-02)

- 协议加入显式版本号；版本不一致时在大厅给出刷新提示并拒绝接入，房主同时对昵称长度、摇杆数值和动作参数数量做边界约束，避免异常消息污染权威状态。
- 螃蟹、鸟、蝴蝶与博美改为房主唯一模拟，并随 10Hz 权威快照下发；客人只应用姿态与可见状态，不再各自随机游荡、刷新或消耗肉块。野生动物继续沿用已有的房主权威快照。
- 世界同步从整份 `SaveData` 重放改为按 `props/campfires/workbenches/crates/fences/beds/drops` 分段比较与增量发送；客人只重建发生变化的模块，降低岛屿发展后的流量、反序列化和场景重建开销。
- 客人断线后，房主冻结其输入并保留稳定会话与个人进度 1 分钟；重新完成一次手动 WebRTC 握手时自动携带恢复令牌，复用原角色 id、背包与生存状态。静态部署没有信令服务器，因此重连仍需房主重新生成邀请码，但不再从新角色开始。
- 创建房间时可选择继续上次保存的联机岛，重新加入的队友按昵称优先认领原角色进度；单机继续不再误生成旧联机队友。会话存档新增稳定 id 与昵称，按约定将 `SAVE_VERSION` 升至 27，旧版本存档直接丢弃。

### M5.1 客人交互提示同步(2026-09-02)

- 房主为每名客人按其权威会话计算头顶交互提示、作业圆环进度与钓鱼提示颜色，并随定向 HUD 快照下发；客人端不再读取本地未运行的交互系统，因此靠近树木等资源时会正常显示工具提示，采集、制作、挖掘、睡觉、烹饪、进食、钓鱼与喝水过程也会显示进度。
- 2026-09-03:修复非房主玩家交互时没有角色动作、采集浆果丛等资源后客人端看不到资源点粒子的问题。玩家快照新增当前作业动作，客人端据此驱动自己和队友的程序动画；采集命中与完成爆发改为由房主按资源点坐标广播，协议版本升级至 5。
- 2026-09-03:修复非房主玩家无法切换自己工具的问题。客人应用定向 HUD 时同步回填权威端的工具等级和当前手持工具，使本地工具循环能识别已经拥有的斧、镐、锄、鱼竿与弓，并保持按钮、角色模型和房主状态一致。
- `hud` 消息结构发生变化，联机协议版本升至 3，避免新旧页面混连导致提示字段缺失。
- 联机玩家死亡后不再退出对局：房主权威等待 3 秒，将该玩家传送到出生点并恢复生存数值，同时清空其背包道具、永久工具与全部穿戴装备；岛屿世界及其他玩家进度不受影响。单机仍沿用死亡确认并清档的规则。
- 继续遵循需求：不新增或运行自动化测试；只执行仓库要求的类型/生产构建与部署验收。

### M5.2 客人端移动与交互反馈平滑(2026-09-03)

- 修复客人端野生动物只更新逻辑坐标、模型保持静止的问题。
- 动物、鸟和宠物继续由房主权威模拟，客人端改为每帧平滑位置与朝向并播放纯视觉动画，避免 10Hz 快照造成跳格。
- 鸟在房主端对最近的任意玩家作出惊飞反应，不再只识别房主。
- 客人端依据相邻 HUD 快照估算交互进度变化，并逐帧平滑进度环，降低 5Hz HUD 快照造成的卡顿感。
- 联机姿态行为发生变化，协议版本升至 4，避免新旧页面混连。

### M7 自动房间信令(2026-09-03)

- 联机大厅由手动交换 SDP 邀请码/回传码改为六位房间码：房主只需创建一次房间，最多三名朋友可同时通过房间码加入，WebRTC offer、answer 与 ICE candidate 由信令服务自动转发。
- 邀请界面针对手机增加二维码、系统分享和可点击邀请链接；邀请链接携带 `room` 查询参数，打开后会自动进入加入房间页并填好房间码，玩家只需输入昵称。
- 新增 `signaling/` Cloudflare Worker + Durable Object 信令服务。每个房间为一个临时 Durable Object，使用休眠 WebSocket，仅中转握手信息，不承载游戏状态和存档；房间十二小时自动过期，房主令牌禁止他人冒充房主。
- `PeerNet` 改为 trickle ICE，不再等待最多四秒收集完整 SDP；游戏消息仍使用原有房主权威 WebRTC DataChannel 直连，GitHub Pages 仍只托管静态前端。
- 默认信令地址为 `https://island-signal.island-870751720.workers.dev`，也可通过构建变量 `NEXT_PUBLIC_SIGNAL_URL` 覆盖。Cloudflare 免费额度用尽时服务拒绝请求，不启用付费计划；STUN 打洞失败的严格网络暂不提供收费 TURN 中继。

### M7.1 国内直连信令(2026-09-03)

- 删除 Cloudflare Worker、Durable Object 及其部署依赖，信令改为浏览器直接连接 EMQX 位于腾讯云上海的国内公共 MQTT WebSocket 节点；不设置境外备用服务。
- 房主随机生成五位数字码并监听该房间的临时上行主题；客人使用随机 peer id 监听自己的下行主题。消息使用 QoS 0 且不保留，只中转 WebRTC offer、answer 与 ICE candidate，不上传游戏状态或存档。
- 客人 DataChannel 建立后立即退出 MQTT；房主在大厅期间保持监听，以便最多三名朋友陆续加入，点击开始游戏后退出 MQTT。正式游戏仅使用房主星形 WebRTC P2P，掉线后需要重新开房，不再支持游戏中途加入。
- STUN 从 Google 切换为国内可直连的腾讯与小米公共节点。严格 NAT 下仍可能无法打洞；项目不提供收费 TURN 中继。

### M7.2 队友音效距离衰减(2026-09-03)

- 客人端补播队友音效时不再固定全音量:`feedback` 事件自带的坐标与本地玩家位置求距离,6 个世界单位内全音量,6~20 线性衰减到零,超过 20(约两三屏)直接不播。
- `Sfx.play` / `GameAudio.play` 新增可选音量系数参数,本地玩家自己的音效不受影响仍走全音量;粒子特效照旧播放。

### M7.3 昵称记忆(2026-09-03)

- 加入房间时输入的昵称持久化到 localStorage(`island.nickname`),下次进入大厅自动带出,想改直接在输入框里改即可;只有非空昵称才会被记住。

### M7.4 断线重进房间(2026-09-03)

- 房主开始游戏后不再关闭信令连接,MQTT 保持在线(掉线自动重连并重新订阅房间主题),断线客人随时可用原房间码重新握手加入。
- 断线席位保留时长从 1 分钟延长到 5 分钟:期间用原房间码重新加入即恢复原角色(背包、状态与位置),超时后席位释放。
- 客人端断线后回到加入房间页,自动带出上次的房间码与昵称并提示可直接恢复;最近房间信息持久化到 localStorage(`island.multiplayer.lastRoom`)。

### M7.3 交互音效仅本人可听 + GM 全房间生效(2026-09-03)

- 交互音效改为「只给发起者自己听」:
  - `feedback` 事件新增 `actor`(发起者玩家 id);客人端只补播 `actor === 本地玩家` 的音效,其余事件只保留粒子特效,彻底移除此前的距离衰减听觉范围逻辑。
  - 房主在 `runNetAction` 结算客人动作期间置 `GameAudio.silent = true`:本地不出声,但噪音通知(`onSfx` 惊动动物)与广播照常,发起音效仍以 feedback 事件回流给客人本人补播。
- GM 指令全房间生效(非房主也能用):
  - `GmSystem` 新增 `GmConfig` 快照类型与 `gmSnapshot()` / `gmApply()`(字段级校验)。
  - 新增网络动作 `gmConfig` / `gmSetTime` / `gmSetWeather`:客人调整 GM 开关或时刻/天气时上行房主权威结算;房主应用后以新 `event: { kind: 'gm', config }` 广播,全体玩家收敛到同一份 GM 配置(无敌/允许死亡/锁白天/风/钓鱼权重/帧率浮层)。
  - `Protocol` 协议版本升至 6;UI 各 GM tab 的开关改为经 `Game.gmSetConfig(patch)` 下发,不再直接改本地 `GmSystem`。

### M7.4 音效归属补全 + 客人自身装备外观回流(2026-09-04)

- 音效只给本人听的补全:此前只在 `runNetAction` 结算期间静音,持续型交互音(砍树/钓鱼/弓箭/吃喝/敲打等)在每帧会话模拟循环里触发,房主仍会听到队友的声音。现房主跑远程会话的模拟时统一置 `GameAudio.silent`(噪音广播照常),只有本地会话出声。
- 客人自身装备外观:HUD 快照本就携带 `equipped`,客人端 `netApplyHud` 现在对比差异后经 `Equipment.restore` 回流应用,自己换装后模型(衣服/裤子颜色、帽子、背包)立即切换,并与房主侧保持一致。

### M7.5 螃蟹躲避所有玩家(2026-09-04)

- 螃蟹此前的逃跑判定只认房主本地玩家,客人靠近无动于衷。`Crabs` 构造参数从单一玩家改为玩家位置列表提供者,房主模拟时传入全部会话的位置快照,任一玩家进入逃跑距离都会惊跑(逃离最近的那个);客人端表现由既有 ambient 快照同步,无需改动。
- 2026-09-04:螃蟹客人端同步补上插值——此前 `netApply` 每 100ms 硬设位置导致一卡一卡,现与鸟/野兽一致:快照只更新目标位姿(`netPos`/`netHeading`,死亡重生时直接落位),新增 `Crabs.netUpdate` 按指数插值逐帧逼近并保持横行动画,Game 客人分支逐帧调用。

### M7.6 交互状态即时下发 + 流量与延迟浮层(2026-09-04)

- 客人交互后音效即时但交互物状态(灌木变空等)最多延迟约 1 秒:原因是音效走即时 `event` 通道而状态只依赖 10 拍(1s)一次的世界快照。现房主在 `runNetAction` 结算完每个网络动作后立即调用 `maybeBroadcastWorld` 下发一次世界快照(哈希去重,无变化不发包),周期快照照旧兜底。
- 新增 `src/game/net/NetTraffic.ts` 全局流量统计:`PeerNet` 在收发统一入口累加字节数;每条 DataChannel 建立后每秒互发通道层 `{t:'ping'/'pong'}`(在 PeerNet 内拦截,不进入游戏消息处理)测往返延迟,按通道 id 记录、断线移除。
- GM 世界 tab 新增「显示网络流量与延迟」开关(GmSystem.showTraffic):`src/ui/TrafficOverlay.tsx` 每秒采样字节差值显示上行/下行速率,延迟取各通道 RTT 最大值(房主多客人时取最差一条),按 <100ms 绿 / <250ms 黄 / 其余红 着色;浮层位于帧率浮层下方。开关随 GM 配置全房间同步,统计只统计本机 DataChannel 收发。

### M7.7 客人端偶现白屏修复:火堆燃料原地同步(2026-09-04)

- 现象:客人端偶现 3D 画面变白,React UI 与游戏逻辑照常,左上角出现浏览器内核给丢失上下文 canvas 的占位图标——即 WebGL 上下文被移动端浏览器因 GPU 内存压力回收。
- 根因:火堆快照含 `fuel`,燃烧时每秒递减 → 房主每秒广播一次 campfires section → 客人 `clear()` + `restore()` 全量销毁/重建火堆模型,持续的 GPU 资源销毁重建最终触发上下文丢失(仅客人发生,房主本地只改数值)。
- 治本:`Campfire.netApplyFuel` 原地更新燃料并在燃/灭切换时调整表现;`CampfireSystem.netApply` 按落点键匹配,火堆集合无增删时只原地同步,有增删才整体重放。`Game.netApplyWorld` 的 campfires 分支改走 `netApply`。
- 兜底:`Game` 构造时对 `renderer.domElement` 监听 `webglcontextlost` 并 `preventDefault`,允许浏览器异步恢复上下文;恢复事件由 Three.js 内部处理并重传资源,画面自愈,不再永久白屏。

### M8 世界同步机制重构:连续数值本地模拟,网络只同步离散事件(2026-09-04)

- 背景:此前世界快照按 section 整体哈希对比、每 1 秒检查一次,且哈希里包含每秒递减的 `regrowLeft`(资源点再生倒计时)与 `fuel`(篝火燃料)——只要有灌木在再生或篝火在烧,props 全量数组(数百项)每秒重播一次,既造成约 50KB/s 的下行流量,也让交互物状态变化最多延迟 1 秒才到客人端。
- 新机制:
  - 连续递减的数值不入增量快照:`props` 下发时剥离 `regrowLeft`(客人在房主再生完成翻转 `ready` 时收增量,≤200ms);`campfires.fuel` 按 15 秒量化,只在跨档或添柴跳变时触发同步,燃尽熄灭由客人本地递减自行得出。
  - 世界快照检查周期从 10 拍(1s)缩短为 2 拍(200ms):剥离漂移数值后哈希只在真实离散变化(采伐/挖除/种植/摆放/掉落)时改变,状态变化自动在 200ms 内同步,无需在各个结算点手动触发广播。
  - 移除上一版「网络动作结算后立即广播」的特例(周期检查已覆盖)。
  - `PropState.regrowLeft` / `PropSave.regrowLeft` 改为可选,本地存档仍始终写入,存档结构无语义变化(不升 SAVE_VERSION)。
- 客人端 `Campfire.netApplyFuel` 原地同步与 `CampfireSystem.netApply` 按落点匹配保持不变,配合量化燃料继续避免模型重建。

### M9 客人端靠近交互物自动切换工具(2026-09-04)

- 问题:非房主玩家空手站在树/大石/蚯蚓旁站定后不会自动切换到对应工具。原因有二:①主循环里 `updateAutoEquip` 被 `if (!this.guestMode)` 跳过;②客人端不跑权威采集模拟,`CollectSystem.getNearby()` 恒为 null,`wantedTool` 判定无从谈起。
- 修复:
  - `CollectSystem` 把「扫描范围内可交互资源点」从 `update` 抽为 `scanNearby()`,客人端每帧只跑扫描不做权威结算,`getNearby()` 恢复可用。
  - `updateAutoEquip` 改为主循环统一调用(不再区分房主/客人),切换动作走 `Game.selectTool`:客人本地先切做预测表现,并上行 `tool` 动作由房主权威结算后经 HUD 快照回流。
  - 客人端忙碌判定:本地制作/进食等系统在客人端不跑,改用 `player.isActing`(房主经 `netApplyPlayers` 下发的作业动作)兜底,作业中不触发自动切换。
  - `netApplyHud` 在下发 UI 前用客人本地 `autoEquipTimer` 覆盖快照中的 `autoEquipProgress`(房主不知道客人端该值),保证工具按钮脉动提示正常。
- 顺带修复:主循环原先对 `updateAutoEquip` 调用了两次(638/676 两处),房主实际等待时间只有 `AUTO_EQUIP_DELAY` 的一半,去重后恢复为 1 秒。

### M10 客人静止时位置残留偏差柔和抹平(2026-09-04)

- 问题:客人位置为本地预测模拟,房主独立模拟同一份输入,两端因输入延迟抖动、碰撞解算分叉、帧积分差异持续产生小偏差;此前只有偏差 >3m 才硬拉回,小于 3m 的偏差在玩家停下后永久残留(两端看到的站位对不上)。
- 修复:客人快照对账改为「本地预测 + 柔和对账」——偏差 >3m 仍硬拉;≤3m 时记入 `netDrift`(x/z 差值),仅在玩家静止期间按指数衰减(`1 - e^-3δ`)把本地坐标悄悄拉向房主快照,几秒内无感抹平;移动中不干预,避免与输入打架。

### M11 客人端体验补齐:自动切工具进度提示、蝴蝶互动、天气同步(2026-09-04)

- 客人端自动切工具无头顶进度提示:该提示由房主 `indicatorFor` 计算经快照下发,而自动切换计时器在客人本地,房主无从得知。客人在 `updateIndicator` 里用本地 `autoEquipTimer` + `scanNearby` 结果自行补上「切换斧子/镐子/锄头/鱼竿…」标签与进度。
- 蝴蝶只被房主惊飞:`Butterflies` 构造参数从单一玩家改为玩家集合提供者(同螃蟹/鸟),取最近玩家做惊飞判定,客人靠近同样惊飞;表现本就经 ambient 快照同步,无需改协议。
- 客人端天气可能与房主不一致:原先客人每 100ms 收到快照就调 `weather.force()`,该方法每次随机重掷本轮起风与风向,导致两端风力/风向表现分叉。`players` 消息新增 `rain`/`windAmount`/`windDirX`/`windDirZ` 房主权威连续值;`WeatherSystem` 新增 `netSync` 进入网络驱动模式,客人本地不再随机轮换,只对权威值做短时常数插值。
- `AGENTS.md` 新增「联机玩法(必读)」章节:后续所有新需求设计时必须考虑联机两端的行为与同步路径。

### M12 客人端表现补齐:箭矢、鱼线、围栏预览、进食特效、提示常驻(2026-09-04)

- 背景:弓/钓/围栏/进食的特效都画在各会话的子系统里,由主循环的权威模拟驱动;客人端不跑该循环,这些画面全部缺失。
- 方案:为相关系统增加「纯表现模式」——结算仍在房主,客人端由快照/同步数据驱动本地复现画面:
  - `BowSystem.netUpdate`:客人端对每个会话跑纯表现弓箭(自动开弓、飞箭、插地),不扣箭、不结算命中与掉落(房主权威);动物位置本就经快照同步,弹道近似一致。
  - `FishingSystem`:新增 `netEnter/netStop/netSyncState`,浮漂与钓线在客人本地起播,阶段(咬钩收竿)与结束由 HUD 快照 `fishingState` 纠正;本地玩家走快照,远程玩家由 `players` 快照的作业动作(cast/fish)驱动起播收线;收竿入包只留水花表现,不做本地入包。
  - `FenceSystem.updatePreviewFor`:客人端对每个会话只刷新围栏/门的落点幽灵预览,放置计时与结算仍在房主。
  - 进食特效:`netApplyHud` 用快照 `eatName/eatProgress` 在客人本地按进度档触发咀嚼声与掉渣粒子(每 1/3 进度一次,与房主 0.5s 节奏一致)。
- 「一觉睡到第二天清晨」等提示在客人端不消失:`Notice` 组件按 HUD 快照对象做 useEffect 依赖,客人端快照反序列化每 200ms 生成新对象导致自动消失计时器一直被重置;改为按 `notice.id` 计时。

### M13 围栏连续放置与预览补全、复活倒计时、熊吼定位音效(2026-09-04)

- 围栏原地连续放置:`pickVertex`/`pickEdge` 原先只在面前约 1 米内选落点,脚边格点放满后就停,站立时只能放一两个;现在允许沿面朝方向走廊(CHAIN_RANGE=4 格)向前延伸,优先接续现有围栏线,站定不动即可连续向前放围栏与门(放置节奏仍为 2s/5s 一个)。
- 围栏预览横杆补全:预览横杆原为 0.46m 短桩,实际围栏横杆跨满相邻两柱约 1m,观感"少了一半";预览横杆改为 0.92m 全跨度,与实际一致,只在对应方向有邻居时显示。
- 联机复活倒计时动态化:`HudSnapshot` 新增 `respawnLeft`(房主权威倒计时秒数),死亡界面由静态"3 秒后"改为实时倒计时数字(每秒带缩放动画),房主与客人一致。
- 熊吼/扑击音效定位:野生动物音效回调带上声源坐标;房主端声源距任意存活玩家 ≤20m 才播放,并经新增 `sfxAt` 事件广播,客人按自己位置同样 20m 判定——熊攻击任意玩家时,附近的每个人都能听到,远处不再全图播报。

### M14 回退原地连续放置,改为原地只放一次;熊吼改为每端按自身位置判定(2026-09-04)

- 回退 M13 的落点走廊延伸(原地会不停连续放置,不符合预期)。改为「原地只放一次」:`useFence/useGate` 成功放置后记录玩家当时位置,挪步超过半格(0.5m)才恢复自动放置;手动从背包放置同样计入。
- 熊吼/扑击音效:M13 的房主端判定误写为「声源距任意存活玩家 ≤20m 即播放」,导致熊攻击远处客人时,20 米外的房主也听到;改为每个端只按**自己本地玩家**与声源的距离 ≤20m 判定(房主本地播放用房主位置,客人经 `sfxAt` 事件用自己位置过滤)。
- M13 的围栏预览横杆全跨度、复活倒计时动态化保持不变。

### M16 围栏放置移动即恢复;复活后持续移动修复;客人端熊击延迟结算(2026-09-04)

- 围栏:原地放置的恢复条件从"距上次放置点 >0.5m"改为"发生过移动即恢复",挪一步再走回原位置也能继续放置。
- 复活后持续移动:死亡界面会卸载虚拟摇杆,死亡瞬间残留的最后摇杆输入一直留在本地(客人端还会持续上行),复活后角色自动不停走;现在死亡瞬间 `setJoystick(0,0)` 清输入并上行。
- 客人端熊击"人先掉血、熊还没到":客人的熊画面比房主权威模拟滞后约 1~2 拍快照 + 插值;现在对客人会话的熊击伤害延迟 0.25s(`WILDLIFE_HIT_DELAY`)结算,等客人画面上熊扑到时再掉血,主人本地结算不变。

### M17 取消客人端熊击延迟结算(2026-09-05)

- 移除 M16 引入的 `WILDLIFE_HIT_DELAY` 延迟队列:熊击伤害一律在房主命中判定时立即结算(客人与房主会话同等处理),`pendingWildlifeHits` 队列及相关代码删除;伤害数字、减速与 `wildlifeHit` 事件广播随结算即时发出。

### M17 客人端复活清摇杆修复;卸下背包收缩并掉落溢出(2026-09-04)

- 客人复活后持续移动:M16 的清摇杆只写在主循环死亡过渡块,但客人端死亡过渡在 `netApplyPlayers` 快照处理里就消费了 `lastDead`,主循环块不再触发;现在快照侧的死亡/复活过渡对本地玩家同样执行 `setJoystick(0,0)`(死亡时并播死亡音效)。
- 卸下背包:背包容量原先只扩不缩,卸下背包后扩容格子仍保留;现在卸下背包(或换上更小的背包)时背包收缩到目标格数(无背包为 `DEFAULT_CAPACITY`=10),被裁掉格子里的物品溢出掉落在玩家附近。掉落由房主/单机端权威生成,客人端经 world 增量快照复现;溢出统一走 `dropOverflow`(与主动丢弃区分来源)。

### M18 客人端受伤表现补播(闪红/音效/伤害数字)(2026-09-04)

- 客人被熊击中的表现原先只打在房主屏幕上(粒子、击中音、伤害数字都在房主端结算),客人只看到快照血量下降,无任何反馈。
- 现在 `applyWildlifeHit` 把受击表现抽为 `playWildlifeHitFeedback`(红色粒子 + 击中音 + 本地头顶伤害数字),房主结算客人被击时广播新增 `wildlifeHit` 事件(带减伤后伤害与是否扑击),客人端收到后本地补播粒子/音效/数字并应用扑击减速;血量仍由 players 快照回流。
- 另在 `netApplyPlayers` 加血量下降检测:客人端任意掉血来源(受击/饥饿/溺水)都会触发角色模型闪红;本地玩家受伤音按 1.5s 节流补播。

### M19 手持鱼竿时不再自动喝水(2026-09-04)

- 自动喝水(`WaterSystem`)原先只看"靠水站定 + 口渴",玩家手持鱼竿站在岸边准备钓鱼时也会低头喝水打断节奏;现在手持鱼竿(`currentTool === 'fishingrod'`)时自动喝水让位,切回其他工具/空手后恢复。结算在房主端,客人表现跟随动作快照,无需额外同步。

### M20 世界状态改为实时字段级增量(2026-09-05)

- 删除房主每 200ms 执行 `collectSave(true)`、按 section `JSON.stringify` 哈希并下发整段数组的机制。权威游戏帧完成后比较轻量世界状态；没有变化时不产生任何世界包，变化在当前帧立即广播，不再等待固定网络拍点。
- 新协议消息为 `worldDelta`，携带单调递增 `revision` 与操作列表。新增实体发送完整实体，删除实体只发送稳定坐标键，已有实体只发送实际变化字段。例如草丛被采集时仅发送该草丛键和 `ready: false`，不重复发送坐标、旋转及其他草丛。
- 客人从 `welcome.state` 建立世界镜像，依次应用增量。资源点的纯字段变化直接原地更新已有 Three.js 实体与外观，不再清空并重建整片资源；结构新增/删除及其他系统暂由镜像生成对应 section 交给现有恢复入口，保证状态一致。
- 再生倒计时继续不进入网络；房主只在 `ready` 真正翻转时同步。火堆燃料继续采用 15 秒量化以避免连续浮点值制造高频包。
- 联机协议版本升至 8，旧页面与新页面会明确拒绝混连。

### M21 事件驱动世界同步与弱网传输重构(2026-09-05)

- 世界同步从“每个游戏帧生成完整快照再比较”升级为真正的事件驱动：`Props`、火堆、工作台、木箱、围栏/门、床和掉落物只在权威状态发生离散修改时通知 `NetHost`，网络层当场递增 revision 并通过可靠控制通道发送；没有变化时没有扫描、序列化或网络包。
- 所有世界实体存档结构增加可选稳定 `id`。新实体创建时生成 id；旧存档恢复时自动补 id 并在下次正常保存时固化，字段具有合理缺省值，因此 `SAVE_VERSION` 保持 28。
- 客人按稳定 id 原地增删改资源、火堆、工作台、木箱、床、围栏/门和掉落物，未变化模型不再销毁重建。木箱内容使用 `slots.N` 路径，只发送实际变化格；普通实体 `set` 只携带变化字段。
- 火堆只同步搭建、添柴、燃尽和挖除事件；燃烧过程由两端本地递减，取消 15 秒量化周期包。添柴立即发送权威燃料值，燃尽发送 `fuel: 0` 校准。
- `worldDelta.revision` 必须严格连续；客人发现缺口会通过 `worldResync` 请求房主发送当前 `worldFull` 基线，应用后再继续增量，避免漏包后永久分叉。欢迎包同时携带当前世界 revision。
- WebRTC 拆为两条 DataChannel：`game-control` 可靠有序，承载动作、世界增量、HUD、事件与重同步；`game-state` 无序且零重传，承载可淘汰的 input/player/animal/ambient 实时状态。状态通道拥塞时每类只保留最新包，控制通道使用高低水位 FIFO 背压。
- 客人输入增加递增 `seq`，房主丢弃重复/倒序输入，并在定向玩家快照中回传 `ackInputSeq`；客人清理已确认历史并保留未确认输入产生的本地预测位移。可靠 heartbeat 独立维持在线判断，避免状态通道严重拥塞时被误判掉线。
- 流量统计改为 UTF-8 应用层实际字节数。联机协议版本统一升至 9，阻止旧页面与双通道/新对账逻辑混连。
- 修复客人端消费权威血量快照后没有更新 `lastHealth`，导致一次掉血被随后每个玩家快照重复判定、角色持续闪红并循环播放受伤音的问题；现在每个快照完成伤害边沿检测后立即推进血量基线。

### M23 房主昵称与主界面按钮加载态(2026-09-05)

- 新增 `src/game/net/nickname.ts` 统一读写本地昵称(localStorage 键 `island.nickname`),加入房间时输入即记住。
- 房主本地会话名由写死的「房主」改为优先使用该昵称;玩家快照本就携带会话名,客人端无需改动即可看到房主真实昵称。房主大厅的玩家列表同步显示自己的昵称。
- 主界面在 React 水合完成前不再渲染「继续游戏/开新档/创建房间/加入房间」按钮,改为显示加载提示,水合后一次性出现,避免预渲染按钮点了没反应。

### M24 房间码改为五位纯数字(2026-09-05)

- 房间码由六位大写字母+数字(去混淆字符)改为五位纯数字,方便口头转述与手机输入;输入框改为数字键盘(`inputMode="numeric"`)。
- `normalizeRoomCode` 只保留数字并截取前五位;加入页自动带出的上次房间码也按新规则过滤,旧格式记录自然失效(房间本身即临时)。协议与同步逻辑不变,仅信令主题命名规则变化,联机协议版本无需升级。

### M25 掉线角色即离场与取消人数上限(2026-09-05)

- 客人掉线(连接关闭或 10 秒无消息)后,其角色立即从世界中移除,所有玩家不再看到滞留的角色;此前角色会在世界里滞留整个 5 分钟断线保留期。
- 断线重连保留:房主在移除角色时采集离场快照(位置/生存/背包/工具/穿戴),凭原恢复令牌在 5 分钟内重连即按快照原样重建角色。快照同时进入待恢复列表,因此房主期间存档不丢该玩家进度;保留期内新客人不会认领仍被保留的角色,过期后可被同名玩家认领。
- 取消 3 位客人(共 4 人)的硬上限:接入与 hello 阶段不再有人数校验,大厅文案同步更新。协议与消息结构不变,联机协议版本无需升级。

### M26 单机中途开启多人模式(2026-09-05)

- 单机玩家可在游戏内设置面板「开启多人模式」:当场创建 `NetHost` 房间并挂接到运行中的游戏,当前岛屿与进度原样保留,朋友凭房间码/二维码随时加入,与大厅建房联机完全等价。
- `Game` 新增 `bindHost(host)`:单机本就是"无 NetHost 的房主权威端",挂接时补做三件事——同步当前 `terrainSeed`、绑定各世界系统的离散变化广播 sink、`host.attach(this)` 启动快照广播;之后来客走正常 hello→welcome 流程(欢迎包携带全量世界快照),无需客人端任何改动。
- 转房主后本地会话名由「我」改为联机昵称;死亡清档/复活规则自动切换为联机规则(死亡不再清档,倒计时重生)。
- 设置面板联机区:未联机时显示「开启多人模式」按钮(创建失败给出错误提示);已联机(含从大厅进入的房主)显示房间码、邀请二维码与「分享邀请」。客人端不显示联机区。
- 邀请链接、二维码与分享文案抽为共享模块 `src/ui/roomInvite.ts`,大厅与设置面板复用。

### M27 入包飞行表现(2026-09-05)

- 采集/捡拾等入包新增纯表现层飞行动画(`fx/ItemFlyFx`):道具模型从交互点沿抛物线飞向玩家后背、缩小消失,到达后再弹拾取飘字(含道具名称)。
- 房主与客人各自本地播放,不走网络:房主端由 `Inventory.onAdd` 触发;客人端沿用 HUD 快照差额触发拾取飘字的既有机制,飞行起点在动作上行时短时记录(约 1 秒)等待快照回流,采集起点取本地扫描到的近旁资源点位置。权威结算与协议均不变。

### M28 入包音效随飞行到达(2026-09-05)

- 入包「叮」声统一移到飞行到达回调(`Game.flushPickups`)本地播放:房主/客人各自随本地飞行表现发声,客人端不再漏听采集入包声;协议与权威结算不变。

### M29 钓鱼收竿阶段移除,中鱼即入包(2026-09-05)

- 钓鱼状态机由 `casting|waiting|bite|catching` 改为 `casting|waiting|bite|reeling`:连点达标瞬间房主即时结算入包,`catching`(鱼模型飞向玩家 0.9s)删除;`reeling` 为约 0.45s 的收线纯表现(浮漂拉回竿梢),入包飞行统一走 `ItemFlyFx` 管线、起点为浮漂落点。
- 客人端:客人本地钓鱼为中鱼表现模式,不再本地拼随机鱼模型;收到快照 `bite→reeling` 即播水花并转入收线表现,同时把入包飞行起点交代在本地浮漂落点(`onCatch` 回调),入包飘字仍由 HUD 快照差额触发;`bite→null` 仍为超时跑鱼。快照在钓鱼阶段变化时立即推送(既有机制),`reeling` 能可靠送达客人。协议字段不变(`fishingState` 字符串值域变化,两端同版本部署无兼容问题)。

### M30 客人端钓鱼与房主时刻对齐(2026-09-05)

- 修复客人端两个问题:中鱼前鱼线提前消失、入包飞行起点不在浮漂处。根因:客人本地钓鱼是随机模拟(等待时长 `rollWait` 本地自抽、档位默认一档),咬钩时刻与房主有出入;原 `netSyncState` 只在本地恰处 `bite` 时才认 `reeling`,快照被忽略后紧随的 `null` 直接清场,`onCatch` 也不触发(飞行起点落到玩家身前兜底)。
- 时刻对齐(主路径):HUD 快照新增 `fishingTier` 与 `fishingWaitLeft`(等待期剩余秒数);客人端 `waiting` 态每份快照以「本地已等时长 + 房主剩余时长」重设等待总额(本地尚在抛竿则暂存,抛竿结束采用),档位跟随房主——预告文字、咬钩时刻、咬钩窗口均与房主一致,误差只剩一份快照间隔(≤200ms)。
- 阶段对齐(兜底):快照 `bite` 强制本地进咬钩(同步连点数);快照 `reeling` 时无论本地处于 casting/waiting/bite 均转入收线并交代飞行起点;房主仍在钓而本地表现已断时先 `netEnter` 重起播再对齐(自愈)。超时跑鱼仍为 `bite→null` 清场。

### M31 修复客人端交互音效重播一声(2026-09-05)

- 现象:客人端钓鱼过程中 whoosh/落水/咬钩/中鱼水花等音效像是播了两次。根因是双通道发声:房主结算客人动作期间本地静音但仍经 `audio.onSfx` 广播 `feedback` 事件(客人端按 actor 过滤后补播,设计如此),而客人端的纯表现层(本地钓鱼模拟 `fishing.update`/`netSyncState` 兜底起播、远程会话 `netEnter`)又以未静音状态把同一批音效播了一遍。
- 修复:客人端三处纯表现路径统一置 `audio.silent`——每帧本地复现循环(弓/钓鱼/围栏预览,含本人会话)、HUD 快照钓鱼对齐(`netSyncState`)、姿态快照远程钓鱼起播(`netEnter`)。本人音效仍由 `feedback` 事件补播(时刻即房主权威时刻),远程玩家交互音效维持「只给发起者本人听」的设计;入包「叮」声仍由本地飞行到达回调播放(房主不为客人结算该声,无重播)。

### M32 入包飞行表现对所有玩家可见(2026-09-05)

- 需求:道具入包的飞行表现(模型从交互点飞向玩家后背缩没)此前只对每个端自己的本地玩家播放,其他玩家看不到别人捡东西/采集入包。音效维持「只给本人听」不变。
- 方案:`NetEvent` 新增 `itemFly { actor, item, count, x, y, z }`(起点=资源点/掉落物/浮漂位置,无记录时兜底为玩家身前),`NET_PROTOCOL_VERSION` 升至 12。房主在 `inventory.onAdd` 对所有会话统一广播(含房主本人,客人端按 `actor` 过滤掉自己的以免与 HUD 快照差额触发的本地飞行重复),并在房主端就地为远程会话播飞行。
- 实现:飞行起点记录 `markPickupOrigin` 从单一本地值改为按会话的 `pickupOrigins` 映射(采集/钓鱼回调对所有会话生效,`pickupDrop` 房主侧也按动作发起者记录);`ItemFlyFx.spawn` 支持按件指定终点回调,终点取该会话玩家后背(远程玩家位置由姿态快照驱动,终点实时跟随);本地与远程共用 `spawnItemFlights` 错峰起飞逻辑,拾取飘字与「叮」声仍只挂在本地玩家的到达回调上。

### M33 钓鱼珍宝(复活石/波塞冬神像)与 Buff 系统(2026-09-05)

- 四档奖池新增「复活石」与「波塞冬的祝福」。复活:房主权威端在死亡瞬间结算复活石(客人端不本地尝试,避免与房主背包权威冲突),碎裂一颗立即在出生点无惩罚复活,`NET_PROTOCOL_VERSION` 升至 13,新增 `NetEvent reviveFx { target }` 补播表现——客人本人当帧不会看到 dead,提示与音效靠该事件;其他玩家看到出生点光效。
- 神像:走既有世界摆件同步管线,`WorldPatch` 新增 `shrines` section(放置/挖除经 change sink 广播增量,客人 `netApply` 重放);放置动作上行 `useShrine`(Actions 表)由房主权威结算。祝福为全岛全局效果,杂物概率削减只发生在房主抽档处(`rollTier(baited, junkCut)`),客人档位本就由快照回流,天然一致。
- Buff 展示:`HudSnapshot` 新增 `buffs: HudBuff[]`(来源:全局 `poseidon` 神像祝福 + 个人 `bearSlow` 熊扑减速)。房主按会话计算(远程会话的减速由房主 applyWildlifeHit 施加,数据同源)随 HUD 快照定向下发;波塞冬祝福所有玩家都显示,熊扑减速只显示在被扑玩家自己的界面上。均为展示数据,不新增权威状态。

### M34 提示私有化/围栏门双向开/摆件四向朝向(2026-09-05)

- 修复:房主本地提示(notice,如复活石苏醒提示)此前包含在 `hudFor` 下发给客人的 HUD 快照里,房主收到任何提示客人也会看到同一条。`snapshotHud` 不再产出 `notice`(类型改为 `Omit<HudSnapshot,'notice'>`),单机/房主本地 UI 与客人端 `netApplyHud` 各自合并本地 notice 后再交 UI,提示彻底私有化;客人本人的复活提示仍由 `reviveFx` 事件触发。
- 围栏门开向:门扇摆向不再固定,由靠近玩家站在门带的哪一侧决定,总是向玩家背侧打开(各端表现本地计算,不影响阻挡同步)。
- 摆件朝向:工作台/床/木箱放置时朝向取玩家面向并量化为上下左右四向(`core/Facing.cardinalRotY`),替代原来的随机朝向/固定朝向;`PlacementSave` 与世界增量 value 新增可选 `rotY`(旧档缺省 0,存档版本不变),客人端 `netApply` 与存档 `restore` 均按 `rotY` 重建。

### M35 弓箭攻击机制重做:客人本地判定命中(2026-09-05)

- 需求:弓从全自动瞄准改为玩家操控——持弓且 9m 内有猎物时移动进入瞄准,沿摇杆方向(360°)显示点状虚线,拉满 0.45s 后松手(停止移动)放箭;箭沿弹道逐帧扫掠判定命中(`core/HitSegment`,平面距离 0.9m);无冷却,放箭动作 0.35s 播完即再射。详见 `docs/bow.md`。
- **联机约定例外:弓箭命中由射手客户端判定,房主只做权威结算**(偏离「房主权威结算」总原则,朋友开黑 P2P 场景接受自报命中的信任模型)。理由:瞄准方向是玩家输入,房主无法替客人决定;客人本地已有完整猎物位置插值,本地判定消除了「客人箭插地、房主那支却命中」的预测分歧。
- 分工:客人本地完整跑瞄准/虚线/箭矢飞行/扫掠命中与命中表现(音效/粒子/扣箭为本地预测),命中瞬间上行 `action: arrowHit {kind, animalId?, x, z}`(新 GuestAction,`Actions.ts`);房主收到后按射者弓等级权威结算——扣 1 支箭、`Wildlife.damage(id)` / `killNearby` 扣血击杀、战利品掉落,死亡/掉落随既有快照与世界增量回流;目标已死或无箭则丢弃(两玩家同时命中同一目标按到达顺序收敛)。
- 移除「房主为每个会话独立模拟射箭」:房主只跑本地玩家的弓,客人的弓只在客人端跑;远程玩家的箭矢在他端不再有飞行表现(其放箭声效仍由 feedback 事件补播,受击/死亡由快照回流)。
- `NET_PROTOCOL_VERSION` 升至 14:`AnimalPose` 新增可选 `species`(房主运行时新生成的动物——如 GM 生成——客人端按快照补建模型)。
- GM 动物生成(同批):GM 面板新增「动物」页,在玩家附近草地生成指定物种;客人端上行 `gmSpawnAnimal {species}` 由房主结算(见 `docs/gm-panel.md`)。

### M36 互相可见的放箭表现(2026-09-05)

- 修复:M35 重做后客人的弓只在客人端跑,房主看不到客人的放箭动作,两端也看不到彼此的箭矢飞行。
- 方案:放箭瞬间广播纯视觉信息——本地玩家放箭时触发 `BowSystem.onShot` 回调(房主直接 `broadcastEvent`,客人上行新动作 `arrowShot {dx, dz}` 由房主 `Game.netArrowShot` 转发事件给其他客人)。各端收到 `arrowShot` 事件(或房主收到动作)后为该玩家调用 `archery.netPlayShot` 复现一支**纯视觉箭矢**(只飞行插地,不做命中判定,避免与射手端判定重复结算),射手本人过滤自己的回声。
- 放箭动作:房主为客人的会话维护 0.35s 放箭动画窗口(`PlayerSession.shotAnimLeft`,主循环内推进并写入 `action` 快照),客人与其他客人经既有姿态快照看到 `shoot` 动作;房主自己的动作本就来自本地模拟。命中结算路径(arrowHit)不变。`NET_PROTOCOL_VERSION` 升至 15。

### M37 修复他人箭矢不飞行/不消失(2026-09-05)

- 修复:M36 的 `netPlayShot` 视觉箭矢只被创建(入队 + 加场景),但两端主循环都只对本地会话调用 `archery.update`,远程会话的弓系统从不更新——箭矢冻结在出手点(看起来像没射出/残留在玩家身上),插地后也永不消失。房主看不到客人放箭动作同源于此链路未被验证走通。
- 方案:`BowSystem` 拆出 `updateVisuals(delta)`(只推进箭矢飞行/插地/消失,不跑瞄准与命中),房主主循环对远程会话、客人端对房主会话每帧调用;本地会话仍走完整 `update`。协议不变(仍是 v15)。

### M38 箭矢消耗统一在放箭瞬间权威化(2026-09-05)

- 修复:客人放箭时本地预测扣箭,但没射中不上行 arrowHit,房主权威背包未扣,快照回流把客人的箭「还」了回去(射空不耗箭);房主自己射箭却在放箭瞬间实扣,两端规则不一致。
- 方案:箭的消耗与命中脱钩——房主收到 `arrowShot` 动作(放箭必发)即在 `Game.netArrowShot` 权威扣一支箭;`arrowHit` 只负责伤害/掉落结算,不再扣箭。客人本地放箭时的预测扣箭保持不变,两端规则一致:射没射中都消耗。

### M39 木剑近战:swordHit 客人本地判定(2026-09-05)

- 新增工具木剑(2 木头、二级工作台制作,不自动切换),持剑靠近动物 1.4m 内每 0.8s 自动挥砍,每刀 1 伤害;详见 `docs/sword.md`。
- 联机沿用弓箭 M35 的约定例外:**命中由持剑客户端本地判定,房主权威结算**。客人本地跑索敌与挥砍表现,命中上行 `action: swordHit {animalId}`;房主校验该会话持剑后 `netPlaySwing` 补放 0.35s 挥砍动作(写入 `action` 姿态快照,各端可见)+ `settleNetHit` 权威结算伤害与掉落(战利品经 DropSystem 掉落在玩家身旁,随世界增量回流)。
- 挥砍音效复用 `chop`:挥剑者本人的音效由房主经 feedback 事件补播(客人本地不播避免重声),近旁动物会被挥砍声惊动。
- `InteractionKind` 新增 `sword`(挥砍期间其他站定交互让位);快照 `toolTiers`/手持 `tool` 自动携带 sword,旧端/旧档缺省 0,协议与存档版本不变。

### M40 修复房主看不到客人挥剑动作(2026-09-05)

- 修复:M39 的 `netPlaySwing` 只设置了动作(`swingLeft`/`slash`),但房主主循环对远程会话只推进弓的视觉(`archery.updateVisuals`),剑没有任何推进逻辑——动作窗口永不结束,且早前版本还会被采集系统的每帧清动作当帧抹掉,房主观感上完全看不到客人挥剑。
- 方案:与弓的 `shotAnimLeft` 同约定,`SwordSystem` 拆出 `updateVisuals(delta)`(只递减挥砍窗口、结束时清动作,不做攻击判定),房主主循环对远程会话每帧调用;动作经既有姿态快照 `action` 字段同步给其他客人。协议不变。

### M41 生物死亡/受击表现(2026-09-05)

- 新增 `src/game/fx/CreatureFx.ts`:受击闪红、死亡倒地(0.45s 侧翻)→ 停留 5s → 1s 渐隐后隐藏。Wildlife/Crabs/Birds 各自持有实例,房主在 `update` 入口、客人在 `netUpdate` 入口统一推进。
- 死亡触发两端各自本地播放,不走网络事件:房主在权威结算死亡(`applyDamage`/`damageNearby`)时播放;客人在姿态快照 `alive`/`visible` 由 true 翻转为 false 时播放(动物姿态 10Hz,延迟最多约 0.1s,可接受)。快照恢复存活(true)时本地 `reset` 还原透明度与侧翻。协议不变。

### M42 种群刷新替代重生 + GM 攻击倍率(2026-09-05)

- 生物死亡后不再定时复活:尸体渐隐结束时房主直接移除实体;每 8s 的种群刷新生成全新个体(全新 id/模型)。
- 螃蟹/鸟的 ambient 同步从数组下标改为稳定递增 id(与野兽同约定):客人端 `netApply` 对未知 id 且存活的姿态补建实体(鸟经 `AmbientPose.variant` 羽色序号保持外观一致),对快照缺失且本地尸体动画已播完的 id 清理实体。协议新增可选字段 `variant`,向后兼容。
- 渐隐时长 1s→5s(倒地 0.45s + 停留 5s + 渐隐 5s)。
- GM 配置新增 `attackMultiplier`(默认 1),作用于剑/弓的伤害结算,随既有 `gm` 事件同步。

### M43 客人端补播生物受击闪红(2026-09-05)

- 受击闪红是短时表现,不进姿态快照:房主在 `applyDamage`/`damageNearby` 结算受击未死时经可靠事件 `creatureHit {target: wildlife|crab|bird, id}` 广播(仅发客人,房主本地已播),客人端按 id 调各系统 `netFlash` 补播;死亡表现仍由姿态快照 `alive/visible` 翻转驱动,不变。

### M44 熊叫与进食完成音效(2026-09-05)

- 熊警戒/暴怒的完整咆哮与扑击前短低吼均沿用 `sfxAt` 带坐标事件;房主权威触发,各端按本地玩家与声源 20m 距离判定可闻性。
- 成功进食在房主扣除食物并结算属性后触发 `eatFinish`;客人的收尾声沿用 `feedback` 事件只补播给实际进食者,中断进食不触发。协议结构不变。
- 进食完成/中断时,房主只在当前动作仍为该食物的进食动作时清空,避免覆盖剑、弓等同帧接管的动作;清空结果通过玩家姿态快照回流给客人。

### M45 水体判定可视化(2026-09-05)

- `NET_PROTOCOL_VERSION` 升至 16；GM 配置新增 `showWaterDebug`，由既有 `gmConfig` 动作与 `gm` 事件同步，全体玩家可同时查看洋红海水/亮绿水洼判定覆盖层。

### M46 喝水与钓鱼水岸规则(2026-09-05)

- 喝水改为脚下实际处于水洼浅水且未游泳；仍由房主的 `WaterSystem` 权威推进，海水不可饮用。
- 钓鱼资格和浮漂落点统一沿玩家朝向查询实际水体：1.5 米内触水才可起竿，浮漂沿同一朝向最远投 8 米且必落水。客人继续上行 `startFishing`，房主以该会话最新姿态权威复核并结算，姿态与钓鱼阶段按既有快照回流。
- 浮漂落点进一步改为在朝向射线 2~8 米内的实际水面随机取样；切换离开鱼竿会由各端表现系统立即终止钓鱼并清掉残留动作，房主快照继续作为客人最终状态依据。

- 2026-09-05：开放远海游泳，房主权威输入模拟与客人预测共用无地图边界的 Player 移动和地形外海底高度；坐标快照、远海存档沿用既有字段。海面各端按本地镜头覆盖，地图边缘保留远海玩家标记，无协议或存档版本变更。

### M47 交互动作统一持有与释放(2026-09-05)

- 修复一类表现 bug:喝水、合成、搭建/升级工作台、搭火堆/烹饪、放围栏/门、锄头挖火堆/工作台/围栏/床/神像/木箱、采集等交互在结束(完成、走开中断、让位)后未清作业动作,角色停下后会一直卡在该动作的姿态。
- 新增 `Player.releaseAction(action)`(只在该动作仍由调用方持有时清掉)与可复用模块 `systems/ActionHold`(交互期间每帧 `hold`、帧末 `commit`,结束时自动只释放自己持有的动作),多 actor 系统(营火/工作台/围栏/床/神像/木箱)按会话状态各持有一个 `ActionHold`;单玩家系统(喝水/合成/采集)用边沿释放。
- 动作清空的结果随既有玩家姿态快照回流给客人,协议不变;同时消除旧实现「结束时无条件 setAction(null) 可能抹掉同帧被剑/弓接管动作」的隐患。

### M48 海水深度着色与轻量波纹(2026-09-05)

- 海面深浅过渡与波纹为各端本地视觉(新增 `OceanDepth`/`OceanMaterial` 模块):深度纹理由各端按同一份地形种子重建,表现一致但不产生网络消息;波纹相位随本地时间推进,允许各端细微差异。
- 不改变水位、水体判定、游泳/钓鱼权威结算;无新增动作、协议与持久化字段,SAVE_VERSION 不变。

### M49 喝水出鳄鱼(2026-09-05)

- 喝水触发鳄鱼的掷骰与生成完全在房主侧:每个会话的 `WaterSystem` 在房主端运行,喝完一轮水回调 `Game.onDrinkRound`(客人本地喝水表现不触发),按 GM 概率在所站水洼 `Wildlife.spawnCrocodile`。
- 鳄鱼为房主权威 AI(追击/伤害/ leash),随既有战斗频段动物姿态快照同步(`AnimalPose.species` 客人端补建),不新增快照字段。
- 新增可靠事件 `crocodileBurst {x,y,z}`:鳄鱼跃出水面时广播,各端本地补水花粒子,距声源 20m 内播 splash 音;出场咬人复用 `wildlifeAttack`(咬合动画)与 `wildlifeHit`(客人受击表现)。
- GM「特殊事件」触发走既有模式:客人上行 `gmTriggerCrocodile` 动作,房主 `gmTriggerCrocodileFor` 结算;概率调整走既有 `gmConfig` 动作 + `gm` 事件全房间同步。协议版本不变。
