# 装备系统(衣服/裤子/帽子/背包)

## 背景

此前玩家只有工具玩法,身体模型是蓝色上衣 + 棕色裤子且不可变更。需要引入装备系统:玩家默认肉色身体,可制作并穿戴四类装备,穿戴后在模型上可见,并由评分决定是否值得换装。

## 需求描述

- 玩家默认肉色身体(躯干与腿均为肤色)。
- 新增四类装备道具,每类两件,共 8 件,均可通过配方制作。
- 装备栏位:衣服、裤子、帽子、背包,各栏位同时只能穿 1 件。
- 评分系统:每件装备有固定评分;制作完成时若评分高于当前穿着,直接自动上身。
- 物品详情页点击「装备」也可手动装备(不限评分)。
- 背包面板在「物品 / 制作」右侧新增第三个 Tab「角色」,展示四个栏位的穿戴情况,支持卸下(放回背包)。
- 模型表现:衣服/裤子直接替换躯干/腿部模型颜色;帽子与背包使用真实程序化模型。
- 背包类装备穿戴后扩容背包格数。

## 设计方案

### 装备定义(`src/game/systems/Equipment.ts`)

| 道具 | 栏位 | 评分 | 属性 | 配方 | 站点 |
| --- | --- | --- | --- | --- | --- |
| 草衣 🍃 | 衣服 | 1 | 防御 +1 | 木头×1+纤维×1 | 手搓 |
| 草裤 🍂 | 裤子 | 1 | 防御 +1 | 木头×1+纤维×1 | 手搓 |
| 草帽 👒 | 帽子 | 2 | 口渴速度 -5% | 纤维×2 | 手搓 |
| 草包 🎒 | 背包 | 2 | 背包扩到 14 格 | 纤维×4 | 工作台 |
| 皮衣 👕 | 衣服 | 3 | 防御 +3 | 皮毛×2+绳线×3 | 工作台 |
| 皮裤 👖 | 裤子 | 3 | 防御 +2 | 皮毛×2+绳线×2 | 工作台 |
| 皮帽 🎩 | 帽子 | 4 | 防御 +1,口渴速度 -5% | 皮毛×1+绳线×1 | 工作台 |
| 皮包 🧺 | 背包 | 4 | 背包扩到 18 格 | 皮毛×4+绳线×4 | 工作台 |

- 一级为草制(树叶/纤维系旧装备全部移除),二级为皮制;皮制品必须在一级以上工作台旁制作。
- 新材料**皮毛 🐾**:猎捕动物掉落(兔×1、羊×2、鹿×2、熊×4),动物掉落改为兽肉+皮毛。

- `Equipment` 类管理各栏位穿戴状态:
  - `equip(kind, inventory, force)`:从背包装备,非 force 时仅当评分高于当前才换;换下的旧装备放回背包。
  - `unequip(slot, inventory)`:卸下放回背包(放不下则失败)。
  - `onChange` 回调通知 Game 更新玩家模型与背包容量(容量只扩不缩)。
- 配方走通用 `RECIPES`(`output` 为装备道具),手搓卡片与工作台面板自动列出。

### 属性机制

- **防御**:`Equipment.totalDefense()` 各栏位叠加;玩家受击结算时伤害减去总防御,最低仍受 1 点伤害(`Game.ts` 熊扑击回调)。
- **口渴速度**:`Equipment.thirstMultiplier()` 各栏位倍率相乘(帽子 0.95);与天气倍率一并乘入 `survival.thirstDrainMultiplier`。
- **受伤飘字**:受击瞬间在头顶(投影坐标)弹出红色伤害数字 `-.damage-pop`,1 秒动画后移除(`GameplayUI.tsx` + `globals.css`)。

### 模型表现(`src/game/entities/Player.ts`)

- 躯干与腿部各占一个独立材质,默认肉色;`setEquip('clothing'|'pants', kind)` 直接改材质颜色。
- 帽子模型挂头顶节点、背包模型挂背后节点,按穿戴种类显隐;卸下即隐藏/恢复肤色。

### 自动上身流程

`CraftingSystem` 每完成一件产物回调 `onFinish(kind)`;Game 侧若为装备道具则调用 `equipment.equip(kind, inventory)`(带评分比较),评分更高即换上,否则留在背包里供手动装备。

### UI(`src/ui/Backpack.tsx`)

- 背包面板双 Tab 扩为三 Tab:物品 / 制作 / 角色。
- 物品详情:装备类道具显示「装备(评分N)」按钮。
- 角色 Tab:按衣服→裤子→帽子→背包列出穿戴道具、图标、名称、评分与「卸下」按钮。

### 存档

- `SAVE_VERSION` 升至 4,存档新增 `equipped` 字段;版本不一致的旧存档按约定直接弃档。

## 迭代记录

- 2026-08-29:首次实现——玩家默认肉色、8 件装备与配方、评分与自动/手动装备、角色面板、帽子/背包真实模型、背包扩容、存档 v4。
- 2026-08-29:修正——帽子/背包模型装备前不再显示;未穿裤子时腿部为深棕色平角裤色,不再是肉色。
- 2026-08-29:装备配方卡片仅在材料足够且评分高于身上装备时展示(手搓卡片与工作台面板共用 `recipeVisible`);工作台标题右侧新增「📖 合成图鉴」按钮,弹出全部配方(产物/材料/站点/装备效果)的图鉴面板。
- 2026-08-29:8 件装备重设计——一级草制(草衣/草裤/草帽/草包)、二级皮制(皮衣/皮裤/皮帽/皮包);新增皮毛材料(动物掉落兽肉+皮毛);装备新增防御(减伤,最低 1)与口渴减速属性;受击时头顶飘红色伤害数字;`SAVE_VERSION` 升至 9。

- 2026-08-29:体验调整——受伤飘字缩小(15px)并贴近头顶(出生点上移量 3.2→2.5、上浮距离缩短);`recipeVisible` 装备卡片在「背包里已存有同栏位不差于产物」时也不再弹出(此前只比较身上穿戴);屏幕左侧的手搓弹出卡片(CraftPrompt)原本走独立过滤逻辑,已改为同样调用 `recipeVisible`。
