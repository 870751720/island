# 玩家掉血屏幕反馈
## 背景
所有血量损失都需要清晰、统一的手机端视觉反馈。
## 需求描述
本地玩家受击、饥饿、口渴、溺水等导致血量下降时，屏幕边缘闪红，中央保持清晰。回血和其他玩家掉血不触发。
## 设计方案
复用 Game 的权威端血量下降检测和客人快照下降检测，独立 DamageScreenFlash 使用透明径向渐变叠层，650 毫秒淡入淡出，700 毫秒内不重复触发，持续扣血形成缓慢脉冲。支持减少动态效果偏好，不拦截触控，不增加 WebGL 绘制或后处理；退出时清理。客人仅由权威快照触发，不从攻击事件重复播放。无新增网络或存档字段。
