# 音乐与音效

## 背景

游戏此前完全没有声音。目标是加入背景音乐与玩法音效,音乐与部分玩法声音由代码用 WebAudio 实时程序化合成，动物叫声、吞咽、喝水和睡觉使用用户提供的压缩 MP3 采样；普通按钮点击使用用户提供的短 WAV 采样，符合手机端性能约束。音乐风格参考久石让:五声音阶的如歌旋律、柔和钢琴音色、温暖铺底。

## 需求描述

- 背景音乐:久石让式调性的生成式配乐,循环不断且每次听都略有不同;白天明亮流动,夜晚低回稀疏。
- 环境声:常驻海浪声;雨天雨声随天气强度淡入淡出。
- 玩法音效:砍树、采石、采集(沙沙声,挥动即响)、手搓/工作台敲打、进食、喝水、抛竿、落水/拉鱼水花、咬钩提示、获得物品(采集收获/捡回/钓到鱼统一同一种)、丢弃、制作完成、死亡。
- 静音开关:游戏内右上角按钮,一键全局静音/恢复。
- 音量设置:开始界面「声音设置」区,音乐与音效两条滑杆独立调节,持久化到 localStorage,下次进入仍生效。
- 遵守浏览器自动播放策略:音频在用户点击「开始游戏」后启动,若上下文被挂起则借下一次触摸唤醒。

## 设计方案

模块位于 `src/game/audio/`,按职责拆分:

- `synth.ts`:合成底层工具——钢琴感拨弦音(正弦+八度三角波+低通+指数衰减)、通用单音、噪声爆发、循环噪声源、简易梳状混响(多抽头反馈延迟,手机友好)。音乐与音效共用,所有输出统一过混响营造海岛空间感。
- `Sfx.ts`:按动作语义分发录音采样与合成音效，录音保持原音高与速度。
- `MusicPieces.ts`:曲目类型与原有三首谱面;原有曲目保留在 GM 中供手动播放。
- `MusicLibrary.ts`:20 首原创曲目,每首 16 小节,两段手写乐句,48–80 BPM;柔和钢琴、五声性旋律、调内和弦与轻薄铺底,营造日系动画的自然、宁静氛围。春季「春芽晨露」「樱雨来信」清新轻盈;夏季「树荫午睡」「蓝湾晚风」舒展摇曳;秋季「落叶书签」「金色归途」温暖怀想;冬季「初雪无声」「雪夜灯火」稀疏安静;钓鱼「浮漂微光」「云影慢渡」「一竿月色」平缓留白。
- `Music.ts`:自动模式优先轮播本人的三首战斗曲,其次为三首钓鱼曲,其次轮播当前风/雨/雪天气的两首曲目,晴天轮播当前季节两首曲目;季节取 GM 覆盖值,auto 时取真实季节。每首两遍再换,退出钓鱼恢复季节曲库,各自动曲库记住本次对局的小节进度,避免反复抛竿总从第一首开头播放。夜晚速度降低约 20%、旋律低八度并增加留白。自动切换在已排程小节结束后生效,保留自然尾音;GM 点选立即断开旧曲输出(含未来排程),从新曲第一小节播放,恢复自动则立即回到该曲库保存的进度,共用混响仅残留极短余响;排程逐小节重新计算速度,避免不同拍号/速度切曲错拍。分解和弦直接读取实际和弦音,保留大小调差异。
- GM「音乐」页显示曲目,可锁定全部 23 首中的任意一首循环,或恢复自动模式;手动选择优先于战斗/钓鱼/天气/季节状态。选择仅本次对局、本机生效,不写存档。按钮至少 44px,曲库两列布局,沿用面板滚动。
- 联机:季节由房主权威推进并沿用快照同步,钓鱼沿用房主确认后回流的本人状态;音乐由各客户端独立合成,不对齐播放进度,战斗状态使用玩家快照可选字段 combat,不新增动作或事件,队友钓鱼不影响本机曲库。
- `Ambience.ts`:海浪(低通白噪声 + 0.09Hz LFO 起伏)与雨(带通噪声,setTargetAtTime 平滑跟随天气强度)。
- `GameAudio.ts`:统一入口。管理 AudioContext 生命周期、总音量/静音、挂起兜底唤醒;对外仅暴露 `play / setNight / setRainIntensity / toggleMute / dispose`。

接入方式沿用 `fx` 粒子系统的注入模式:各玩法系统(Collect/Crafting/Workbench/Eating/Water/Fishing/Drop)在事件发生处调用 `audio.play(...)`;`Game` 负责创建与销毁,并在主循环同步昼夜与雨声、检测死亡播放音效。UI 层 `GameplayUI` 提供右上角静音按钮(44px 触控区)。

天气曲库:刮风「风过树梢」「远风来信」,下雨「檐下听雨」「雨后的微光」,下雪「雪落如羽」「围炉等雪」。均为 16 小节原创慢速钢琴,每小节一至两个旋律音、三次分解和弦。天气使用已同步的雨雪强度与未叠加阵风包络的风量,超过 0.55 进入,低于或等于 0.35 退出;过渡重叠时雪、雨、风依次优先,防止临界值反复切曲。

夏季「树荫午睡」「蓝湾晚风」分别调整为 52/50 BPM、四拍长乐句,降低旋律音区和密度,伴奏每小节由八次减少至三次;原有「夏日之风」也降至 54 BPM 并使用稀疏伴奏。所有曲目 ID 保持稳定。

战斗曲库:「林间警讯」76 BPM、「逆潮而行」80 BPM、「守护微光」72 BPM,各 16 小节、两段原创乐句,采用小调色彩与钢琴分解和弦,保持柔和音色并增强行进感。GM 中可独立选择并循环。

战斗状态仅影响配乐:房主 AI 正在追击该玩家的敌对动物、该玩家受到动物攻击、弓/剑有效命中动物会刷新 6 秒战斗缓冲;空挥、空射、采集、饥饿和溺水不触发。没有追击或命中持续 6 秒后脱战,死亡立即视为非战斗。进入/退出战斗立即切换音乐,各曲库保留进度;手动锁定音乐时不自动切换。战斗计时不入存档,不改变伤害、仇恨或其他玩法结算。

## 饮用音效与交互生命周期

`drink` 使用完整约 1 秒的喝水采样。食物饮用与水洼/净化器每半秒触发时，上一段尚未播完则忽略重复请求，避免反复截断。每份饮品或每轮喝水结束、中途取消仍通过停止通道以 50 毫秒淡出；饮品完成不额外播放 `eatFinish`。客人按 HUD 状态变化清理当前食物音效。

## 菜单点击与确认音

### 游戏内按钮反馈

- 游戏内按钮与菜单统一使用「鼠标点击1-xys20070412.wav」采样，覆盖 HUD、背包、配方与制作、设施面板、数量加减、地图、设置、拍照、GM 和死亡界面的按钮。
- `gameButtonAudio.ts` 在 `GameplayUI` 根容器捕获主键 `pointerdown`，兼容触屏按钮的 `preventDefault`、停止冒泡与按下即卸载。一次按下只播放一次，长按数量连发不重复播放；键盘和辅助技术的零 detail 点击单独补声，普通触屏后续 click 不重复播放。
- 只识别按钮、按钮角色及显式标记的点击目标；禁用按钮、手动音效按钮、摇杆、滑杆、空白区域和画面拖动不播放。按钮声表示按下反馈，具体操作成功仍由玩法结果反馈。
- 点击声遵循本机音效音量，零音量不创建音频上下文；首页海浪/菜单声音开关不影响游戏按钮。短音沿用 `UiAudio.ts` 的释放机制，切换或退出界面不截断尾音。
- 单机、房主、客人均只对本地按钮操作发声，不进入玩法噪音通知，不上行动作、不广播事件、不由快照补播；原有玩法权威结算保持不变。

### 开始菜单

- 普通点击使用「鼠标点击1-xys20070412.wav」采样，覆盖创建/加入房间入口、设置形象、打开荒岛传承、天赋与等级预览、返回和大厅普通按钮。
- 确认操作统一使用试听第二版 B「清脆确认」：659.25 Hz 短音后接更重的 987.77 Hz 主音，叠加八度泛音与下落低音；2 毫秒起音、短暂保持后快速收尾，总长约 0.35 秒。用于保存形象设置（含回车提交）、确认传承加点、开新档及放弃旧档确认、开始/继续游戏、创建免费房间、提交加入房间与房主开始游戏。
- `UiClickSample.ts` 内嵌原始 WAV 字节（10,678 字节、22,050 Hz 单声道、约 0.239 秒），首次播放解码并缓存 AudioBuffer，不改变音高或速度，不依赖外部资源路径，兼容 Pages 子路径与离线发行版。`UiAudio.ts` 管理普通点击和确认音的独立短音上下文，先唤醒再排程；所有短音结束后关闭。`useMenuAudio` 仅管理首页海浪，页面卸载停止海浪时不会截断按钮声音，也不延迟界面切换。
- 菜单容器通过 `buttonAudio` 捕获普通按钮点击；业务确认按钮声明手动播放，在本地校验通过后发声，传承在成功加点后发声，避免点击声与确认音叠加。禁用按钮及无效提交不发声。沿用菜单声音开关和音效音量，音量为零时不创建短音上下文。
- 房主和客人的菜单音仅响应各自本地操作；网络请求提交时的确认音代表已接受本地点击，不表示联网成功。失败继续使用大厅状态文字提示，快照、队友操作和客人自动进入游戏不补播确认音，不新增网络协议或存档字段。


## 录音采样与压缩

用户提供的羊、牛、吞咽、喝水、睡觉、熊咆哮分别映射 sheepHurt、bisonHurt、eatFinish、drink、snore、roar。熊扑击前的短低吼 bearGrowl 保持独立合成。前五份转为 64 kbps 单声道 MP3，保留原采样率、完整时长与音高，不裁剪、不调速；熊咆哮原码率已为 64 kbps，仅去除元数据，不重新编码。原文件保持在用户下载目录。压缩后总计 66,105 字节，相比原始 173,241 字节减少约 61.8%；有损压缩的听感由本地试听确认。

GameplaySampleData.ts 内嵌压缩字节，兼容离线入口与 Pages 子路径。GameplaySampleAudio 在游戏音频启动时预解码并缓存，播放完成断开节点；解码未完成时跳过当次事件，不补播过期声音。喝水与睡觉同类采样不重叠，睡觉保留原有触发节奏与自然尾音。其他动物命中可重叠，以保留多个动物同时受击反馈。播放共用音效音量、静音与位置衰减，不新增玩法或图鉴内容，不改变存档版本。


## 进食吞咽与进度对齐

固体食物总耗时由 EatingTiming.ts 的 EAT_TIME（1.5 秒）统一定义。每份开始推进后，读取 eatFinish 的实际 AudioBuffer.duration，使用 Web Audio 时钟在「剩余进食时间减采样时长」时启动吞咽，采样自然结束对应进度条终点；不再在消耗完成时补播。排程较晚时从采样对应偏移播放尾段，不调速、不拉长进食。吞咽阶段停止咀嚼音，保留进食动画与碎屑。移动或游泳取消时停止已排程或正在播放的吞咽，每份连续进食独立排程。饮品不播放吞咽收尾，食物消耗与恢复仍在完成时权威结算。

客人根据房主 HUD 的 eatProgress 与同一总时长排程吞咽；切换食物、进度回退、取消或死亡时清理，避免旧排程串入下一份。吞咽仅在本人本地排程，房主不再广播完成后的 eatFinish 反馈，因此不会重复播放。网络延迟或卡顿仍可能造成可见进度与音频的微小偏差。图鉴内容与存档结构不变。
