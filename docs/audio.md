# 音乐与音效(程序化生成)

## 背景

游戏此前完全没有声音。目标是加入背景音乐与玩法音效,全部声音由代码用 WebAudio 实时程序化合成,不引入任何音频文件,不增加加载体积,符合手机端性能约束。音乐风格参考久石让:五声音阶的如歌旋律、柔和钢琴音色、温暖铺底。

## 需求描述

- 背景音乐:久石让式调性的生成式配乐,循环不断且每次听都略有不同;白天明亮流动,夜晚低回稀疏。
- 环境声:常驻海浪声;雨天雨声随天气强度淡入淡出。
- 玩法音效:砍树、采石、采集(沙沙声,挥动即响)、手搓/工作台敲打、进食、喝水、抛竿、落水/拉鱼水花、咬钩提示、获得物品(采集收获/捡回/钓到鱼统一同一种)、丢弃、制作完成、死亡。
- 静音开关:游戏内右上角按钮,一键全局静音/恢复。
- 音量设置:开始界面「声音设置」区,音乐与音效两条滑杆独立调节,持久化到 localStorage,下次进入仍生效。
- 遵守浏览器自动播放策略:音频在用户点击「开始游戏」后启动,若上下文被挂起则借下一次触摸唤醒。

## 设计方案

模块位于 `src/game/audio/`,按职责拆分:

- `synth.ts`:合成底层工具——钢琴感拨弦音(正弦+八度三角波+低通+指数衰减)、通用单音、噪声爆发、循环噪声源、简易梳状混响(多抽头反馈延迟,手机友好)。音乐与音效共用,所有输出统一过混响营造海岛空间感。
- `Sfx.ts`:按动作语义命名的音效合成(每次播放带随机音高抖动避免机械感)。
- `Music.ts`:生成式配乐,三首久石让风格骨架的原创小曲轮换(每首连续两遍后换下一首)。参考其代表作的技术语汇(调性、和声进行、拍号、织体,不受版权保护;旋律为原创,不照搬原曲):「夏日之风」(《One Summer's Day》式,C 大调 4/4 慢速流动)、「海边圆舞曲」(《人生的旋转木马》式,A 小调 3/4 圆舞曲织体)、「森林絮语」(《风之甬道》式,F 大调田园风)。夜晚自动放慢 20%、低八度、旋律留白更多。音乐走独立总线,整体音量压在音效之下。前瞻式排程(setInterval 200ms,排 0.6s)。
- `Ambience.ts`:海浪(低通白噪声 + 0.09Hz LFO 起伏)与雨(带通噪声,setTargetAtTime 平滑跟随天气强度)。
- `GameAudio.ts`:统一入口。管理 AudioContext 生命周期、总音量/静音、挂起兜底唤醒;对外仅暴露 `play / setNight / setRainIntensity / toggleMute / dispose`。

接入方式沿用 `fx` 粒子系统的注入模式:各玩法系统(Collect/Crafting/Workbench/Eating/Water/Fishing/Drop)在事件发生处调用 `audio.play(...)`;`Game` 负责创建与销毁,并在主循环同步昼夜与雨声、检测死亡播放音效。UI 层 `GameplayUI` 提供右上角静音按钮(44px 触控区)。

## 迭代记录

- 2026-08-28:首版。程序化音效 14 种、昼夜变奏生成式配乐、海浪/雨环境声、静音开关。
- 2026-08-28:配乐改为三首久石让风格骨架的原创小曲轮换(4/4 流动 / 3/4 圆舞曲 / 田园风),并整体调低背景音乐音量(独立音乐总线)。
- 2026-08-28:作业声改为挥动开始即响(采集为两段沙沙声),所有获得物品的时刻(采集收获/捡回/钓到鱼)统一为同一种拾取音。
- 2026-08-28:拾取音改为清脆双音(两声上行);采集沙沙声加大音量、频段下移,保证手机外放可闻。
- 2026-08-28:雨声音量减半;开始界面新增「声音设置」(音乐/音效滑杆,持久化),音乐与音效分独立总线,游戏内静音按钮移除(设置统一收在开始界面);海浪/雨归入音乐总线。
- 2026-08-28:修复挥动开始判定在计时自增之后导致采集音效从不播放的 bug。
- 2026-08-28:沙沙声重做为「随机微爆裂颗粒串」(高频段 14 个随机分布的极短嚓声 + 低频身量层),更接近真实草叶/树叶质感。
- 2026-08-28:按草丛拟音的典型配方再调:「shhh」轻扫气声(带通 900→2600Hz 上扫)垫底 + 10 个脆嚓颗粒叠上,去掉低频闷响层,保持干爽中高频质感。
- 2026-08-28:修复手机外放听不到 drop/munch/drink 的问题——三者原频段(70–1100Hz)低于手机扬声器下限,整体上移至 400Hz 以上并补高频接触噪声(drop 落地「嗒」、munch 频段升至 2400Hz、drink「咕咚」改 620→300Hz 下滑+吞咽轻响),音量同步加大。采集声重做为「拨草丛」质感:一记频段下滑(2400→700Hz)的软「唰」+ 6 个细碎刮擦颗粒。
- 2026-08-28:按材质拟音重做三种作业声:砍树=高频劈裂脆响+木体共振短咚+低频冲量(入木的「笃」);采石=两个失谐高频三角波振铃+石面崩裂噪声(镐击的「叮」);采集=三层错位、频段各异的软带通噪声叠出起伏(拨草叶回弹的「唰」)。synth 的 noiseBurst 新增可选 Q 参数。
- 2026-08-28:再调砍树与采石:砍树去掉高频脆响,压低共振频段、加重低频,改为沉闷的「咚·笃」;采石去掉金属振铃与沙沙感,改为硬质中频敲击+低频回震+少量崩落碎屑的「磕」。
- 2026-08-28:镐凿大石恢复为金属振铃「叮」版;碎石堆采集从通用 pick 声中拆出,新增 pickStone(三颗石子翻动的「咔啦」),草丛/浆果/灌木仍用「唰」。
- 2026-08-28:砍树改又短又密的低频闷击(缩短衰减避免混响拖出空旷感,两层贴近低音叠加);碎石改为极短干硬「咔哒」(去掉滑频噪声,消除水感);喝水重做为「咕咕」水泡音(四声由低滑高的极短吞咽)。
- 2026-08-28:喝水声改为每轮喝水开始时播放(落在喝水过程中而非结束时);死亡音效按经典「下行轮廓」惯例重做——五个音符逐个降低、间隔递减,像皮球弹跳到静止,末了一声低音落地(参考 Zelda/Mario 式死亡 jingle 的通用设计语义)。
- 2026-08-28:进食声按拟音「分层咬合」重做——每口由脆断瞬态(高频咔嚓)+ 湿软挤压(低频 squish)+ 低频短击三层叠成,节奏与音高随机;喝水声从四声扩为八声铺满整轮 2 秒。
- 2026-08-28:进食声再调——去掉低频「咚」(怪声来源),改为一口内 4 个极短微碎裂(嚼碎颗粒感,Q=2 共振带通)+ 一记轻湿挤压,每次只咬两口(本身每 0.5s 循环);新增长音效中途切断能力(Sfx.stop / GameAudio.stop),取消喝水/中断进食立即静音。
- 2026-08-28:按用户反馈去掉进食的脆响颗粒,改为纯柔湿咀嚼——每口两层中低频带通挤压音(750/1300Hz)的「mua」声。
- 2026-08-29:新增添柴音效 stoke(木柴抛落两声闷响 + 火焰腾起的低频「呼」),添柴时播放。
- 2026-08-29:新增烹饪滋滋声 sizzle(高频油爆气声垫底 + 4 粒随机爆裂脆响),烹饪期间每 0.8 秒循环播放。

## 迭代记录

- 2026-08-30:新增「受伤」音效(`hurt`,低频下滑闷哼+噪声冲击),血量下降时与角色模型闪红同步播放;Game 层做 1.5 秒节流,饥饿/溺水等持续掉血时不会连响。
- 2026-09-05:熊叫声去除锯齿波的电子感,改为约 1 秒的柔和起声、喉腔共鸣与沙哑下沉尾音;警戒/中箭暴怒播完整咆哮,扑击蓄力改播短低吼。成功吃完食物后新增「吞咽 + 轻快上行双音」收尾;中断进食不播放,联机时只向实际进食者补播。
