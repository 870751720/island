# 天气系统

## 背景

荒岛求生的环境表现需要更丰富的变化。昼夜循环解决了时间维度,但白天的画面节奏单一;天气是最自然的环境压力与氛围来源,且在低多边形风格下实现成本可控。

## 需求描述

两种天气,晴雨随机轮换:

- ☀️ 晴天
- 🌧️ 雨天:雨滴粒子、明显压暗去饱和

要求:

- 开局随机天气(晴雨各半)。
- 天气随时间随机切换,晴雨交替(不连续同种);每种持续 50~110 秒。
- 切换时强度平滑过渡(约 10 秒),不做突变。
- HUD 时钟旁显示当前天气。
- 移动端友好:粒子数量与灯光调制成本可控。

## 设计方案

### 模块

- `src/game/systems/WeatherSystem.ts`:天气状态机。持有 `sun`/`hemi`/`scene` 引用,在 `DayNightSystem.update` **之后**执行,对昼夜系统输出的天空色、灯光强度、太阳颜色做一层调制(乘暗系数 + 颜色 lerp)。
- `src/game/fx/Rain.ts`:雨丝。单个 `THREE.LineSegments`(550 条,长度 0.3~0.6 随机,下落速度 14)在玩家周围 44×44 区域内下落循环,水平跟随玩家,透明度随雨量渐变,雨量为 0 时隐藏。
- `src/game/SurvivalSystem`:新增 `thirstDrainMultiplier`(仅作用于口渴),由天气驱动;雨天淋雨口渴消耗 ×0.6。

### 数值

| 天气 | 天空调制 | 灯光 | 玩法 |
| --- | --- | --- | --- |
| 晴 | 无 | 无 | 无 |
| 雨 | lerp 向深灰蓝 `#5f7280` | 太阳 ×0.45 / 半球 ×0.55,颜色偏冷 | 口渴 ×0.6 |

### UI

`HudSnapshot` 新增 `weather` / `weatherLabel`,`Hud` 在时钟后显示 `☀️/🌧️` 标签。

## 迭代记录

### 2026-08-28 迭代二:精简为晴雨两种,开局随机

- 移除雾天及 `scene.fog` 维护,天气简化为晴/雨交替轮换。
- 开局随机天气(晴雨各半),初始强度直接到位。
- 雨天加强:粒子 350→700、增大加亮、下落提速;天空更深灰蓝、压暗幅度加大。

### 2026-08-28 迭代三:雨丝化并放慢

- 雨滴由 Points 方点改为 LineSegments 竖直雨丝(长 1.2),更贴近真实雨的形态。
- 下落速度 34 → 14,数量调整为 500,透明度略降避免过密。

### 2026-08-28 迭代四:雨丝改短

- 雨丝长度 1.2 → 0.3~0.6 每条随机,消除长直线的出戏感;数量 550、透明度微调保持密度。

### 2026-08-28 初版

- 实现晴/雨/雾三种天气的状态机与平滑过渡。
- 雨滴 Points 粒子跟随玩家;雾用 `THREE.Fog` 随强度插值。
- 雨天口渴消耗减缓(淋雨);HUD 显示天气标签。
